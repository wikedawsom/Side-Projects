using System;
using System.Net;
using System.IO;
using System.Net.Sockets;
using System.Threading;

namespace WikeBot
{
    class BotMaster
    {
        public void MainLoop()
        {
            bool exit = false;
            while (!exit)
            {

            }
        }
    }
}
